name: Release Windows Installer

on:
  push:
    tags:
      - "v*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows-installer:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4

      - name: Setup MSYS2 + MinGW + SDL2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-SDL2
            mingw-w64-x86_64-pkg-config
            make

      - name: Build game
        run: make

      - name: Copy runtime DLLs next to exe
        run: |
          cp /mingw64/bin/SDL2.dll SDL2.dll
          # Filets de securite : si jamais le static-link MinGW ne couvre
          # pas tout (cas exotique), on embarque aussi ces dlls. Sinon
          # ignore par l'installer via skipifsourcedoesntexist.
          cp /mingw64/bin/libgcc_s_seh-1.dll libgcc_s_seh-1.dll || true
          cp /mingw64/bin/libwinpthread-1.dll libwinpthread-1.dll || true

      - name: Install Inno Setup
        shell: powershell
        run: |
          choco install innosetup --no-progress -y

      - name: Compile installer
        shell: cmd
        run: |
          "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\element_dungeon.iss

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: ElementDungeon-Setup
          path: installer/out/ElementDungeon-Setup-*.exe

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: installer/out/ElementDungeon-Setup-*.exe
          generate_release_notes: true
